# Fallback local: usado APENAS se o build não conseguir falar com o servidor
# de versionamento (sem internet, token errado, etc.).
# A versão real vem do endpoint /api/public/bump-version no Lovable Cloud,
# que mantém um contador atômico (2.1, 2.2, 2.3, ...) que nunca reseta.
versionBase=2